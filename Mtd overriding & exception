##Method overriding occurs when a child class provides its own implementation of a method that is already defined in the parent class. 
class Parent:
def show(self):
print("This is Parent class method")


class Child(Parent):           //Child class inherits from Parent
def show(self): # overriding the parent method       //Both classes have a method named show()
print("This is Child class method")


# Creating object
obj = Child() //The Child class overrides the Parent method
obj.show() //When we call obj.show(), the child version is executed



//Exception handling
try:
a = 10
b = 0
print(a / b)
except:
print("Error occurred!")


//Write a Python program to perform division of two numbers using exception handling.Use try and except to handle division by zero. Use else to display a message when no error occurs. Use finally to display a message that always executes. 
a = 10
b = 2
print(a / b)
except ZeroDivisionError:
print("Cannot divide by zero")
else:
print("No error occurred")
finally:
print("This will always execute")

